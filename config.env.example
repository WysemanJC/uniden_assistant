# Uniden Assistant Configuration (example)

# Backend URL (leave blank to auto-detect FQDN)
BACKEND_URL=

# SQLite is used for both core and favourites data.
# No database environment variables are required.
